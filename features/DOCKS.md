# Tosh Company Features Index

This folder contains only approved, implementation-ready ecosystem features. App-specific implementation features stay in the corresponding app repository. Raw ecosystem ideas remain in `genesis/` until the user promotes them.

## Current Status

No ecosystem feature is promoted to `features/` yet. The project is in documentation and architecture discovery. The initial proposals are:

| Code | Raw proposal | Status | Owner |
|---|---|---|---|
| F01 | Tosh ecosystem foundation: identity, app registry, shared contracts, and onboarding | Raw in `genesis/F01-raw/` | Root ecosystem |
| F02 | Tosh Marketplace: public web catalog, publisher flow, host components, and trust boundaries | Raw in `genesis/F02-raw/` | Root ecosystem plus marketplace implementation |
| F03 | Cross-app host integration: account, capability, installation, update, and rollback contracts | Raw in `genesis/F03-raw/` | Root ecosystem plus each host app |

Promotion requires an agreed DOCKS document, dependencies, verification plan, and a user-approved cycle. Until then, these are proposals, not implementation instructions.